= simple_form_for(@category) do |f|
  = f.error_notification
  .form-inputs
    = f.input :name, required: true, autofocus: true
    = f.button :submit, t('helpers.submit.category.create'), data: { disable_with: t("loader.saving") }
